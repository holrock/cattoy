<% if flash[:notice].present?  %>
alert('<%= flash[:notice] %>');
<% end %>
var html = '<%= j(render('form', history: @history, remote: true)) %>';
$('<%= "history_form_cat_#{@history.cat.id}" %>').html(s);
